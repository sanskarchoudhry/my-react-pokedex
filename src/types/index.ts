export type Ability = {
  ability: {
    name: string;
    url: string;
  };
  is_hidden: boolean;
};

export type SpriteUrl = {
  other: {
    "official-artwork": {
      front_default: string;
    };
  };
};

export type Species = {
  name: string;
  url: string;
};
